# =============================================================================
# 开发/测试环境附加依赖
# 安装: .\.venv\Scripts\python.exe -m pip install -r backend\requirements-dev.txt
# 建议先安装 requirements.txt, 本文件基于其之上叠加
# =============================================================================

-r requirements.txt

# ----------------------------- 测试工具 -----------------------------
pytest>=8.2.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx>=0.27.0,<1.0.0
# 集成与契约测试使用独立 PostgreSQL 测试库，驱动由 requirements.txt 提供。

# ----------------------------- 代码质量 -----------------------------
ruff>=0.6.0,<1.0.0
mypy>=1.11.0,<2.0.0

# ----------------------------- 热重载增强 ---------------------------
watchfiles>=0.22.0,<1.0.0
