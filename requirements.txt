# =============================================================================
# 项目核心依赖清单 (生产环境)
# 目标运行环境: Python 3.12+
# 安装: .\.venv\Scripts\python.exe -m pip install -r requirements.txt
# =============================================================================

# ----------------------------- Web 框架 -----------------------------
fastapi>=0.115.0,<1.0.0
pydantic>=2.8.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0

# --------------------------- ASGI 服务器 ----------------------------
# Uvicorn: 开发默认服务器，基于 uvloop/httptools 高性能实现
uvicorn[standard]>=0.30.0,<1.0.0
# Hypercorn: 可选替代方案，支持 HTTP/2 与 Trio，如需可取消注释
# hypercorn>=0.17.0,<1.0.0

# --------------------------- 数据库 / ORM ---------------------------
# SQLAlchemy 2.0 异步 ORM 核心
sqlalchemy[asyncio]>=2.0.30,<2.1.0
# 异步 PostgreSQL 驱动 (FastAPI 异步场景首选)
asyncpg>=0.29.0,<1.0.0
# 同步 PostgreSQL 驱动 (Alembic 迁移 / 同步脚本场景使用)
psycopg2-binary>=2.9.9,<3.0.0
# 异步 ORM 依赖的协程桥接层 (SQLAlchemy asyncio 必需)
greenlet>=3.0.0,<4.0.0

# --------------------------- 数据库迁移 -----------------------------
alembic>=1.13.0,<2.0.0

# ----------------------------- 配置管理 -----------------------------
python-dotenv>=1.0.0,<2.0.0
